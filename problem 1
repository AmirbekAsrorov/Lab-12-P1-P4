// #include <iostream>
// #include <string>
// #include <vector>
// using namespace std;
//
// int hashCodeString(const string& s) {
//     const int a = 33;
//     int hash = 0;
//
//     for (int i = 0; i < s.length(); ++i) {
//         hash = hash * a + (unsigned char)s[i];
//     }
//
//     return hash;
// }
//
// int main() {
//     int numStrings;
//     cout << "Enter the number of strings: ";
//     cin >> numStrings;
//
//     vector<string> inputs(numStrings);
//
//     cout << "Enter " << numStrings << " strings:\n";
//     for (int i = 0; i < numStrings; i++) {
//         cin >> inputs[i];
//     }
//
//     cout << "Problem 2 String hash codes (a = 33):\n";
//     for (const string& str : inputs) {
//         cout << "String: " << str << " -> " << hashCodeString(str) << endl;
//     }
//
//     return 0;
// }


#include <iostream>
#include <vector>
using namespace std;

int hashCodeInt(int key) {
    return key;
}

int main() {
    vector<int> keys;
    int numKeys, key;

    cout << "Enter the number of keys: ";
    cin >> numKeys;

    cout << "Enter " << numKeys << " integer keys:\n";
    for (int i = 0; i < numKeys; i++) {
        cin >> key;
        keys.push_back(key);
    }

    
    for (int key : keys) {
        cout << key << " -> " << hashCodeInt(key) << endl;
    }

    return 0;
}
